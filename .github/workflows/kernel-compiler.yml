name: Building New Kernel

on: [push]

env:
  GIT_SECRETB: ${{ secrets.GIT_SECRETB }}
  GIT_SECRET: ${{ secrets.GIT_SECRET }}
  BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
  GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
  ZIP_PASS: ${{ secrets.ZIP_PASS }}
  TZ: Asia/Jakarta

jobs:
  build-begonia-r-oss-uv:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'begonia-r-oss-uv'

  build-begonia-r-oss-uv-d:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'begonia-r-oss-uv-d'

  build-begonia-r-oss-std:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'begonia-r-oss-std'

  build-begonia-r-oss-std-d:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'begonia-r-oss-std-d'

  build-begonia-r-oss-stock:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'begonia-r-oss-stock'

  build-begonia-r-oss-stock-b:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'begonia-r-oss-stock-b'

  build-begonia-r-oss-stock-c:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'begonia-r-oss-stock-c'

